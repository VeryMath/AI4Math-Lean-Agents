# Placeholder eval harness (Phase 2/3). Spec: ../eval/tasks.yaml
# Usage (future): python -m scripts.run_eval --config eval/tasks.yaml
# Enforce quota.max_usd_per_run == 5 before any live API calls.
echo "eval_run placeholder: see eval/tasks.yaml (max_usd_per_run: 5). Implement in ~/numina-lean-agent Phase 2+."
exit 0
